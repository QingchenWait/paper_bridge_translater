# 开发记录

## v0.3.2 同版本修订 · 文件指纹与目录定位 · 2026-09-23

范围限于新 PDF 导入的重复确认、初始指纹持久化和从阅读区进入文档管理的目录定位。版本、IndexedDB 版本 2、存档版本 2、运行依赖均不变。

- `utils.md5Blob` 复用已安装的 @noble/hashes/legacy.js，按 2 MiB 分块异步读取原始 Blob；同一不可变 Blob 以 WeakMap 复用计算 Promise，失败清除缓存。不从画布、导出的编辑 PDF 或显示名称计算指纹。
- `storage.addDocument` 在开启事务前计算初始 MD5，将 initialMd5 随文档与源 Blob 一同保存，涵盖导入和生成译文。`findDocumentsByMd5` 按全库匹配；旧记录逐份从原始 Blob 补算，事务重新读取最新文档/文件及删除标记后仅补充字段，保留 updatedAt 与其他元数据，不复活删除中的记录。
- `App.importFiles` 固定本次文件列表和目标目录，所有入口共用串行导入队列；支持 Web Locks 的环境再通过同源命名锁串行多个标签页的导入检查。无 Web Locks 时保留单页面队列，避免连续拖入时绕过确认。
- `performImportFiles/confirmDuplicate` 使用既有圆角模态窗口与下载的 x/check 图标，明确“是/否”；关闭、Esc、点击遮罩等同“否”，只跳过当前文件，继续同批其余文件。确认后在 .pdf/.PDF 后缀前追加“副本”，普通上传始终以新 UUID/rootId 创建，不按 MD5 复用文档/编辑/对话。生成译文保留原共享组规则。
- `showLibrary` 只在从阅读工作区进入时读取当前 activeId 的最新目录，无有效活动 PDF 则传入根目录。`LibraryView.open({folderId})` 在加载偏好之后应用目标、清空旧搜索，沿用原选择清理；无参数的管理页内刷新保留当前目录。两套布局共用入口，不改其他导航样式。
- `archive.js` 接受缺少 initialMd5 的旧存档，对新字段校验 32 位十六进制格式；现有快照、恢复与云合并自然携带字段，原有 SHA-256 完整性检查不变。新建文件不会用哈希充当 ID。

新增验证包括 MD5 与 Node crypto 独立对照、分块边界、编辑后初始值保留、同字节独立文档、旧数据补算/时间戳保留、补算期间删除不复活、存档往返和非法字段拒绝；浏览器覆盖是/否/关闭、批量、无 Web Locks 连续拖放、多标签并发、外链根目录副本、桌面/手机最新目录与空阅读区根目录。原先用于测试独立文档的合成 PDF 补充各自 Title，避免仅改文件名而意外创建同字节测试文件。

验证全部通过：`npm run check`、71 项单元测试、独立 5193 端口上的完整 89 项浏览器回归、`npm run build` 与 `npm run test:dist`。桌面目录定位和手机确认弹窗/目录截图已检查。生产子路径验证覆盖初次加载 MD5 模块、PDF 导入、中文编辑导出、持久化恢复和高亮；dist 仍为 278 个文件、约 16.4 MiB，保留原有 Vite 主块体积提示。版本保持 0.3.2，未新增 npm 依赖，原有 5173 服务保留。

## v0.3.2 · 有道客户端入口与中文详细释义 · 2026-09-23

范围仅为单词词典的可用性判定、候选检索及详细释义中文显示。包/锁文件、侧栏、关于页更新为 0.3.2；数据库、存档格式、运行依赖不变。保留已有的窄屏调整、用户修改的首页文案及基础服务说明。

- `dictionary-fallbacks.js` 增加有道 result.code/data.entries 解析，只接受与所选单词忽略大小写匹配的 entry，避免把 suggest 补全建议当作目标词。正则按词性标记拆分 explain，词义内部的分号保留；缺失字段和接口省略内容不编造。
- 客户端通过现有 Tauri 标识或显式 `__PAPER_BRIDGE_DICTIONARY_FETCH__` 宿主钩子启用有道优先查询；普通网页跳过。钩子接收 `(url,{signal})`、返回 Promise<Response>，仅用于有道请求并须遵守取消/超时信号；没有钩子时客户端尝试原 fetch。基础设置增加“网页版不支持、客户端需 CORS 代理”的说明，不新增配置字段或后端。
- `hasDictionaryDetails` 要求词性与非空 definition 同时存在。主要词典仍并行请求，按完成顺序验证/转换；空 meanings、空解释及翻译失败的候选不阻止检查另一个主要词典。中文简短词义成功后，缺少详细释义仍继续 FreeDictionaryAPI→3325，全部失败才结束。
- `chineseDictionaryEntries` 在副本上逐条转换英文解释、映射中文词性，保留词义结构、音标、原文例句、近义词和词形。正文解释转换成功前不发布英文到 UI；转换失败继续寻找中文词典结果。已有中文简短词义可提前显示，全部失败如实报告，不调用 LLM。
- `translateDetail` 复用 basicTranslate，当前服务优先，然后尝试其他免 Key/完整凭据服务；网页版跳过已标记不可用的火山方式。请求范围只在当前单词，失败服务本次不重复尝试，相同文本复用 Promise，不更改全局设置。全部逐字条转换保留现有分段规则，无截断或拼接分隔符解析；`definitionSources` 记录实际服务并由现有词典署名区显示。
- 所有词典/翻译复用选词取消信号，取消后不启动下一候选、不发布旧更新。共享前端流程用于桌面和移动端，无阅读/编辑布局变动，继续复用原图标和提示组件。

接口核对：真实无 Origin 请求返回 HTTP 200 与用户指定结构；带网页 Origin 的请求被有道以 Invalid CORS request 拒绝。客户端宿主入口采用可控 transport 验证，当前未制作原生安装包或 CORS 代理，不宣称已完成真实客户端联调。

验证全部通过：语法检查、66 项单元测试、完整 82 项浏览器回归、静态构建及生产子路径检查。新增覆盖有道词性拆分/匹配过滤、原生优先及网页跳过、空详细释义持续检索、主要词典空壳不遮挡另一个候选、英文转中文前不展示正文、翻译 API 回退与重复解释去重、转换失败转向中文词典、转换期间取消。桌面/手机详细释义截图已检查；原有 PDF 编辑、下载、会话和存档用例全部通过。dist 共 278 个文件、约 16.4 MiB，保留原有 Vite 主块体积提示；未修改运行依赖或用户存储。首次沙箱内语法检查因 Node 子进程权限受限，使用允许的执行环境复查成功。

## v0.3.1 同版本修订 · 中文词典备选与窄屏入口 · 2026-09-23

范围仅为单词词典备选及移动端/窄屏打开按钮、顶栏名称。版本保持 0.3.1，运行依赖、存档及数据库结构均不变。

- 对照用户指定官方站点及 FreeDictionaryAPI OpenAPI，新增 `dictionary-fallbacks.js` 纯转换模块。FreeDictionaryAPI 使用 `/api/v1/entries/en/{word}?translations=true`；按 zh/zho/cmn 语言代码从 senses/subsenses 中取中文，排除其他语言和纯拼音，去重后适配 entries/phonetics/meanings/forms。3325 使用 `/api/word/{word}`，只接受 code=200，映射 british/american、cx、jbjs；不虚构接口未返回的词形或音频。
- `translation.js` 保留主要英文词典的并行竞速与现有基础服务优先级。中文成功后不请求新增备选；缺少中文时按 FreeDictionaryAPI→3325 串行查询，各沿用 6.5 秒词典超时。请求失败、429、未收录或无中文继续下一家；用户取消则停止链路并禁止旧回调更新。主要英文定义优先，补充音标和词形不被较晚的主请求/空词形覆盖；只有中文也可展示，不调用 LLM。
- `ui/assistant.js` 仅在词典区补充真实来源署名、词条/许可证 HTTPS 链接，并按实际中文来源标记；不再把备选结果标成 MyMemory。所有词典文本经过已有转义，非法来源 URL 只显示文字。
- `main.js` 只改独立 mobile-header 的标题；`mobile.css` 仅在 ≤960px 隐藏文件卡片行右侧的 .open-pdf，保留顶部按钮的三项菜单。没有更改 desktop.css 或桌面品牌与入口。
- 增加转换单元测试和查询顺序、原词典成功不回退、无中文/限流回退、来源字段、晚到结果及取消回归；原打开菜单测试在窄屏改用顶部保留的入口。继续复用已下载图标，不新增美术或运行依赖。

真实浏览器从本地页面直接调用两家公开词条均返回 200，获得中文释义并成功转换；FreeDictionaryAPI 按 Origin 返回允许来源，3325 返回 `Access-Control-Allow-Origin: *`。使用公开 attention 单词验证，未访问用户文档/密钥；网络和服务额度仍由各站点决定。390px 窄屏实测无横向溢出、标题正确、卡片行按钮隐藏，1920px 桌面按钮显示且 mobile-header 隐藏，截图已检查。

验证全部通过：`npm run check`、62 项单元测试、完整 73 项浏览器回归、`npm run build`、`npm run test:dist`。浏览器回归使用独立 5193 端口，原有 5173 服务保留；静态子路径验证覆盖 PDF 导入、中文编辑导出、持久化恢复和高亮。dist 为 278 个文件、约 16.4 MiB；保留原有 Vite 主块体积提示。本轮四份开发文档及 THIRD_PARTY.md 已同步，包与界面版本均保持 0.3.1。

## v0.3.1 · 2026-09-23

本轮只修改打开 PDF 入口、标准标记/笔迹导出、Noto 字体子集、批注浮栏及标签标题。运行依赖、IndexedDB 版本 2、存档版本 2 不变；用户已有文档和设置不迁移、不清空。

- 新增 `ui/open-pdf.js`：按钮下方的三项自绘菜单，复用已有下载图标、folder-tree 和 modal；文档树按目录分组、文件夹优先、可折叠。点击文件复用现有首次切换锁，本地选择保持同步点击授权。手机入口使用同一菜单，触控行高单独在 mobile.css 配置。
- 新增 `pdf-links.js` 校验 HTTP(S)、字面及路径 `.pdf` 后缀、可选安全文件名；固定后缀不放入输入框。下载可取消，响应先经原 importFiles/PDF.js 验证再入库。`importFiles(files,{folderId})` 允许链接导入显式指定根目录，本地文档管理导入仍使用当前目录。无代理或后端，远端须允许 CORS。
- `pdf-comments.js` 增加 `appendPdfMark`，将 Underline/StrikeOut/Highlight 的 QuadPoints 和 Ink 的单一 InkList 子路径写入标准注释，AP 只保存对应标记外观。高亮使用浅色透明 Multiply，一笔 Ink 对应一个可独立删除的注释，不绘入页面内容流。既有注释、文本框和形状导出继续沿用原路径。
- 字体根因有两处：@pdf-lib/fontkit 1.1.1 的 CFFSubset 将 CFF.length 写入 offSize；反复使用既有 FontDict 时又误取最后一个子集字典及其局部 Subrs，导致中英混排出现 callsubr 越界。新增 `pdf-fonts.js`，仅针对本项目原始 NotoSansSC-Regular 实例修正编码字段和原 FD→子集 FD 映射；不修改 node_modules、不换字体。适配署名与 MIT 文本随 public/licenses/FONTKIT.txt 分发。
- `repairNotoCffFonts` 仅识别 Noto 且文件头损坏的旧应用子集，按既有 ToUnicode / CID 顺序从同一原始 OTF 重建流，原字体名、字符宽度及 Unicode 映射保持原样；无法安全匹配时显式报错，不替换其他字体。导出先 flush，再修复旧流；已正确嵌入的字体在后续导出中保持字节不变。
- `text-annotations.js` 将浮栏挂在对象自身，以局部定位和同一次布局同步移动，移除全局 fixed 跟随及位置过渡。文字模式只显示字号增减和删除，点击字号保持 textarea 焦点并合入当前编辑历史，删除独立可撤销；空输入不因改字号留下空对象。
- `index.html` 标签标题更新为“纸间 · 文献翻译”；包版本、锁文件、侧栏及关于页统一 0.3.1。公共样式继续使用浅色、蓝紫、圆角与柔和阴影；窄屏不改变原底部导航和阅读分屏。

新增验证覆盖桌面/手机菜单锚点、文件选择、嵌套文档树、链接校验与根目录入库、取消/无效 PDF 不入库、文本编辑三按钮及删除撤销、逐指针步长浮栏同步、两轮中英混排字体导出、标准文字标记和整笔 Ink 回读。另用临时 PyMuPDF 独立检查原字体提取/渲染与整笔删除，不引入 Python 运行依赖。

验证全部通过：`npm run check`、58 项单元测试、独立 5193 端口上的完整 66 项浏览器回归、`npm run build`、`npm run test:dist`。两轮导出的字体流可由独立引擎提取，第一轮正确子集在第二轮中保持字节相同，PDF.js 无字体/CFF/子程序警告；MuPDF 将连续手绘识别为一个 Ink 对象，可一次删除。外链测试使用可控响应，不宣称任意外部网站均允许跨域。桌面/手机文档树、外链表单、编辑浮栏和二次导出渲染截图已检查；截图关闭动画以捕获窗口最终布局。dist 共 278 个文件、约 16.4 MiB；Vite 主块约 1501 kB，存在分包体积提示，静态功能检查通过。现有 5173 开发服务保留，未读取用户文档或密钥。

## v0.3.0 同版本修订 · 原位批注与标准注释 · 2026-09-23

范围限于批注/文本框的建立编辑、源文强调、标准 PDF 注释导出，以及“模型与 API”控件高度；版本、数据库、存档格式和运行依赖均不变。

- 新增 text-annotations.js，由 PdfViewer 委托原位 textarea、size/text 两种状态、左右宽度手柄、顶部移动与跟随浮栏。创建直接聚焦，Enter 换行，点外部结束，空内容移除；输入中保留 DOM 和光标，不被其他标记的重绘替换。原有按住拖动与取词优先规则保留。
- 自动宽度保留原 45% 页宽上限，手动 width 可超过上限但不越出页面；手动宽度在重载/继续文字编辑时保持，重置删除 width 以恢复自动布局。字号、边框、宽高和多行文字纳入存档校验，旧数据按缺省值兼容。
- 浮栏用新下载的 a-arrow-up/a-arrow-down、rotate-ccw 和原有边框/删除图标，移动手柄使用 move。仅文本框有边框按钮，删除无确认且可撤销；文字会话和一次宽度拖动各形成一条 before/after 历史。
- 新增 annotation-writes.js：不可变快照、串行组事务、所有者/删除标记检查；失败回滚，防止写入已删文档。导出、备份及切换等待输入写入完成，活动编辑也可实时备份。
- selection-actions 的 note 创建返回空草稿，不调用外部文本弹窗；main.js 移除专用批注输入/编辑弹窗回调。派生 note-anchor 下划线使用记录颜色，以几何命中触发发光，覆盖层不拦截源文选取。
- 新增 pdf-comments.js，按 ISO 32000 建立 Underline/Text + Popup、Parent、UTF-16 Contents、QuadPoints、颜色、日期与稳定标识。下划线 AP 只绘制标记，批注正文不绘入页面；既有 Annots 原样保留并追加。页面文本框继续使用记录字号、手动宽度和可选边框导出，显式换行与长词/CJK 拆行不丢字符。
- 紧凑表单只作用于 #provider-form，桌面和手机规则分开写在 desktop.css/mobile.css；其他设置、聊天和新原位输入不应用这些高度。

验证：语法检查、52 项单元测试、完整 59 项浏览器用例、静态构建和生产子路径检查全部通过。新增覆盖空新建、输入未退出即刷新、多行、固定宽度及重置、字体/边框/删除撤销、触控双点/宽度、源文发光、存档几何与跨阅读引擎注释回读。原有 PDF/会话/文件管理/选词/拖动/云设置用例通过。dist 为 277 个文件、约 16.4 MiB。新增四个下载图标，未手绘 SVG。

外部兼容性实测：PDF.js 与 MuPDF 均读取到完整中文多行 Contents、Underline 与 Popup 链接；原页面文本没有批注正文。Chrome 自带阅读器能渲染下划线并在悬停时显示原生批注弹窗，但其当前弹窗固定使用 ANSI 字体，中文会漏显。已对照 PDFium CreatePopupAnnot/GeneratePopupAP：它忽略文件自带 Popup 外观，因而不能靠标准 Popup 字体配置修复这个宿主问题。本轮没有运行 Acrobat，不宣称对其各版本实测；格式符合其标准注释结构。此实际限制已向用户说明并写入 README。

PyMuPDF 仅装在 .cache 临时验证目录，不是应用依赖；Chrome 验证使用合成 PDF 和临时资料。现有 Noto 子集字体兼容警告仍可能出现，本轮原生批注不依赖该字体，中文 Contents 完整；文本框沿用既有字体方案。

## v0.3.0 同版本修订 · 2026-09-23

本轮范围为进度动画、模型/基础翻译配置自动保存、完整凭据备份和启动窗口；版本号、依赖、数据库/存档格式均不变。

- `base.css` 原减少动态效果规则把 duration 压到 0.01ms，却保留无限循环，是环形进度异常高速的原因。两种环形类统一使用 --loading-spin-duration=1.2s；减少动态效果时为 1.8s，其他缩短动画限制单次，消除无限高速循环。
- `settings.js` 所有设置改用同一串行队列，捕获入队时不可变补丁，执行时从最新值合并；`saveBasicTranslation` 也委托该队列。新增 flushSettings 供备份等待，防止 LLM、基础、引导等写入互相覆盖。显式空 API 数组不再从旧单 API 别名复活最后删除项。
- `settings-panel.js` 监听 input/change/valuechange，新增、填写、切换协议/能力/默认服务、移除和模型列表选择均静默保存；重复值去重，输入框不重绘，卡片名称同步更新。关闭时从原表单自身读取协议，防止移出 DOM 后全局选择器丢失 Responses 值。顶部添加“保存当前设置”，等待提交且保持窗口；保留底部原保存并关闭入口。
- `ui/basic-settings.js` 实时保存账号/Secret 和测试状态，不增加成功提示。修改凭据仍会取消旧测试；关闭/换页保存最后输入，旧回调不更新新页；可选项不变时不重绘默认下拉。手动保存和错误反馈保留。
- `archive.js` 在一致快照前等待自动保存队列完成；includeSecrets=true 原样打包全部设置，包括多家模型 Key、三家基础 keyId/secret、WebDAV 凭据和其他设置行；false 继续剥除凭据，不修改本机配置。
- 启动窗口改为指定标题、功能行、工作室/作者链接，进入按钮居中、“直接进入 APP”新文案，开关与文字同处右下角，右上角提供关闭；作者新标签复用官网打开适配。关闭本次窗口不改变“不再显示”的既有选择。
- 共用样式在 base.css；桌面启动窗口宽度与按钮间距、手机标题字号及 API 操作按钮换行分别维护在 desktop.css / mobile.css。继续复用下载的 Lucide 图标，未新增手绘资源或依赖。

新增单元测试验证不可变补丁与交错写入、完整设置/凭据加密备份往返、不含凭据备份和清空 API 防复活。新增桌面/手机用例验证输入后自动保存且无提示、顶部保存不关闭、关闭后协议保留、最后一次输入立即导出、两种系统动画偏好、新启动布局/作者外链/关闭与偏好。已有基础测试改为验证新 Secret 已自动保存；所有旧引导测试仅同步入口文案。

验证已全部通过：`npm run check`、47 项单元测试、独立 5193 端口上的完整 53 项浏览器用例、`npm run build`、`npm run test:dist`。重点包括普通/减少动态效果两种速度、桌面/手机启动布局与作者链接、API 配置静默自动保存、关闭后 Responses 保留、勾选凭据后的即时加密备份回读。启动窗口及设置截图已人工查看。静态产物 dist 共 277 个文件、约 16.4 MiB；所有测试使用临时浏览器资料和虚构密钥，版本仍为 0.3.0。

## 0.3.0 · 2026-09-23

本轮仅新增非 LLM 基础翻译管理及相应阅读区入口、配置持久化、凭据备份处理。数据库/存档版本仍为 2，不修改已有 PDF、批注、会话或源项目。

### 模块与协议

- 新增 `basic-translation.js`，集中五种服务、参数构造与解析；`translation.js` 保留原入口并委托此模块，单词仍使用词典，中文释义补充跟随基础引擎，百度单词补充走通用接口。
- Google 使用用户指定文章中的 translate.googleapis.com/translate_a/single，client=gtx/dt=t，无 Key、代理或额外后端。
- 百度按风格选择 fieldtranslate(domain=academic) 或 translate，签名分别为 MD5(appid+q+salt+domain+secret) / MD5(appid+q+salt+secret)。官方端点不提供普通 fetch CORS，但两个接口已验证支持 JSONP；限制 origin/路径、回调唯一、取消/超时清理，GET 保守分段至 1500 UTF-8 字节，当前页面队列间隔至少 1050ms。
- 阿里为 mt.cn-hangzhou.aliyuncs.com 上的 TranslateGeneral RPC POST，Version=2018-10-12，FormatType=text/Scene=general，HMAC-SHA1/RFC3986 排序签名。火山为 translate.volcengineapi.com 的 TranslateText、Version=2020-06-01，官方文档指定 Region=cn-north-1（不是其他云产品常见地域）；V4 SHA256 签名包含隐式 Host、日期和实际 JSON 字节摘要。
- MyMemory 保留 450 字节分段，Google/百度 1500，阿里/火山 4500；依次翻译所有段，不静默截断、不改投其他服务。各服务按应用语言码转换，百度学术非中英方向明确报错。
- MD5 从新依赖 @noble/hashes@2.4.0 按需加载，SHA/HMAC 使用 Web Crypto；锁文件和分发许可证同步。UI 复用已下载 Lucide 图标。

### UI 与状态

- 新增 `ui/basic-settings.js`，嵌入现有设置的第五个子页。默认选择器列出两家免配置服务及已保存完整凭据的三家 API；栏目默认折叠，展开教程/输入/并排测试保存按钮，密钥使用独立眼睛开关。
- 连接测试是真实短句请求，依据当前风格，只有有效译文才显示绿点“可连接”；凭据变化取消任务并增加修订号，旧成功不能覆盖新输入。仅保存最近成功时间及风格，提示不代表永久在线或永久免费额度。
- settings/app 新增 basicTranslation；旧数据默认 MyMemory。`saveBasicTranslation` 串行按最新值合并不同 API 与默认设置；保持 LLM 配置字段语义。设置弹窗内未保存草稿可跨子页保留，离开/关闭时停止测试。
- 阅读区选择器列出所有基础服务和 LLM，基础引擎默认与新子页同步。没有改全文翻译和问答路由。
- 教程外链复用网页新标签及既有 Tauri 系统浏览器桥接；公共控件在 base.css，横屏/窄屏间距与触控尺寸分别维护在 desktop.css、mobile.css。
- 普通备份和云同步清除基础服务 keyId/secret/connection；完整凭据存档可恢复；旧存档缺字段不清空本地，新存档缺密钥不覆盖本地完整账号，不把不同 ID 与旧 Secret 混合。

### 调研与验证

已读取用户指定的 Google 思路及三家官方动态文档，并对照阿里、火山官方 SDK 签名器。浏览器真实调用 Google 短句成功；百度无效凭据 JSONP 返回可读的 52003，阿里响应可被浏览器读取；火山 OPTIONS 允许跨域，但本次无效凭据的实际错误响应缺少 Access-Control-Allow-Origin，浏览器只能给出连接失败。没有用户真实密钥，因此不宣称已完成三家账号成功鉴权或额度实测。

新增签名测试用独立 Node crypto 对照浏览器 Web Crypto/MD5，覆盖 UTF-8、特殊字符、完整规范请求、地域、响应错误、长文本/取消、凭据备份和并发保存。新增浏览器用例覆盖桌面/手机折叠、教程新标签、眼睛、测试进度、修改中止、状态持久化、默认选择、五引擎阅读区路由及百度风格切换、JSONP 超时清理。

现有 5173 端口开发服务保留；其热更新 URL 与测试动态 import 的裸 URL 会形成两个设置缓存实例，回归使用独立 5193 端口干净服务器（临时配置在 .cache，不改变发布配置）。测试选区模拟同时派发按下/松手，以遵守既有去重及松手翻译规则。旧断言同步新增 Google 选项；恢复窗口使用当前测试源，保存/标注/撤销在界面确认完成后再刷新或重选，动态标签尺寸使用可重试测量，避免读取被页码进度更新替换中的 DOM。

最终验证：`npm run check`、44 项单元测试、`npm run build` 与 `npm run test:dist` 均通过。浏览器全量回归有 46 项通过，最后一项手机标签高度检查读到重绘间隙的空 DOM；改为可重试的同等高度断言后单独复测通过，47 项覆盖均已通过。生产子路径验证包含本地 Worker/资源、中文编辑 PDF 导出和持久化恢复，dist 共 277 个文件、约 16.4 MiB；MD5 按需块约 4.3 kB。桌面/手机新页面截图已人工查看。未使用任何用户真实文档或密钥。

## 0.2.1 · 2026-09-23

范围限于本轮六项下载、交互、启动引导和 API 配置反馈。数据库/存档仍为版本 2，无新增 npm 依赖；不改会话上下文、翻译协议、文件移动/删除范围及源项目。

### 结构与实现

- 新增 `document-download.js`：在同一个只读事务读取文档、原始 Blob 和批注快照；筛掉已删除或其他文档的记录，再按需复用既有 `exportAnnotatedPdf`。独立解析器在 finally 释放，下载过程中切换阅读标签不会销毁导出所需实例。无编辑直接复用原 Blob，错误不静默退回原文件。
- 文档库单份/两份与 `buildLibraryZip`、阅读工具栏、`Assistant.exportTranslation` 已有译文下载均接入共用导出。保存位置仍在异步模块加载/生成前选择一次；源文件及分离的编辑记录保持不变，完整存档继续保留可编辑数据。
- `main.js` 在窗口 pointerdown 捕获阶段关闭形状菜单、激活工具；`PdfViewer.beginShapeFromPointer` 将尚未命中墨迹层的首次按下转发给同页画布，不丢失首笔，也阻止同一事件启动对象拖动。
- 原文选择手势给阅读容器增加 selecting-text 类；覆盖元素暂时 pointer-events:none，拖动入口同样校验此状态。鼠标/触控均释放或失焦后清理，原有直接按住元素拖动、手绘/橡皮擦和松手翻译逻辑保留。
- 新增 `providers.js`：八种准确预设、完整主机名到固定官网密钥页映射、网页/Tauri 打开入口；`settings.js` 兼容重导出 PROVIDERS，不套用新模板修改既有配置。新增 hideOnboarding 默认为 false，旧 onboardingDone 不代表主动选择“不再显示”；开关单独保存且跨引导步骤保留。
- `settings-panel.js` 复用 custom-select 显示添加菜单；每次切换先捕获原草稿。Key 的眼睛按钮只改显示类型，切换配置后隐藏；获取按钮监听未保存的 Base URL，并在点击时重读，未匹配网址禁用，不把 Key 拼入地址。
- `components.js` 增加 providerLogo，下载七家厂商的 Lobe Icons 固定提交 SVG 和 Lucide eye-off。下载脚本、第三方来源、构建许可证复制同步维护。
- 共用组件样式在 base.css，桌面 >960px / 移动 ≤960px 的密度和触控尺寸分别放在 desktop.css / mobile.css；窄屏设置可以纵向滚动，Key 行不会撑破窗口。

### 验证与边界

已通过 `npm run check`、36 项单元测试、42 项浏览器回归。新增测试回读工具栏、单份、ZIP 和译文下载的 PDF 文本，确认包含批注/文本框、排除已删除/其他文档的编辑，并校验源文件哈希不变。现有两份下载单次选址、冷导出、取消/错误不重复下载及备份恢复继续通过。

真实鼠标与 Chrome 触控事件验证形状菜单按下即关且首笔保存、选区穿过三种覆盖元素且松手才翻译；原有拖动、橡皮擦、缩放对齐、查找、历史测试通过。引导测试覆盖默认每次显示、跨步骤开关、旧设置兼容及主动关闭；桌面与手机均验证八种预设、LOGO 加载、Key 可见性、未保存地址新标签、未知地址禁用和配置持久化，截图已人工查看。

外部 API 使用模拟响应，未调用用户真实账号。预设按指定名称填写，不宣称已验证模型权限。Tauri 官网打开桥接已对照官方 opener 源码并通过模拟宿主测试；当前未交付/运行原生安装包，宿主需要 opener 插件及 URL 权限。浏览器/系统决定默认浏览器最终使用的新标签策略。

`npm run build` 与 `npm run test:dist` 均通过；生产检查覆盖子路径部署、本地 Worker/资源、PDF 导入、中文编辑导出、持久化与高亮恢复。发布产物为 dist/，共 275 个文件、约 16.4 MiB。PDF.js/fontkit 原有字体兼容警告仍可能出现，已有中文/旋转/高 DPI 回归和导出回读均通过。

## 0.2.0 · 2026-09-23

范围为本轮文档管理更新、配对文件规则，以及标签切换、形状选择、保存位置三项优化。用户补充指定：不支持选择器的浏览器直接默认下载，不额外提示；已遵循，不把权限取消/写入错误当作再次下载理由。

### 结构与迁移

- 新增 `library.js` 的目录/选择/移动/下载计划和精确删除事务，新增 `ui/library.js` 承载文档管理页面与移动/删除弹窗。桌面与移动样式继续分别维护。
- 数据库由 1 增量升级为 2，仅创建 `folders`、`deletions` 两表，不清除旧表。documents 增加可选 folderId，旧值按根目录处理；rootId 保持稳定文档组身份，即使原文被删除也不改变会话 ID。
- 原文移动带译文跟随；移动整个文件夹保持后代层级，并把目录外关联译文归入原文所在目录。下载和删除仅按显式选中集合/所选文件夹后代，不按 rootId 扩大文件范围。
- 新译文写入事务读取源文档的最新目录；来源已删除则拒绝创建。原文删除后 `contextDocument` 回退到当前或其他存活组成员，全文译文记录的 source/documentId 引用转向存活成员。
- 存档格式升级为 2，版本 1 缺少的新表补为空；校验文件夹层级、删除记录所有者及允许缺失物理原文的逻辑组。文件夹和删除记录参与 ZIP、加密备份、WebDAV 原流程。

### 删除安全边界

确认前生成包含对象 ID 和路径的清单；确认后与重新读取的有效选区取交集，不补入新增后代，已移出目录且未直接选中的 PDF 保留。目录只有在删除后没有任何剩余文件/子目录时才移除。循环/无效层级中止操作。重复父子选择去重，空选择无操作。

一个事务删除精确 PDF metadata/Blob/所选 PDF 批注并写入删除标记；共享会话/消息/全文历史只在当前组完全没有文件时删除，否则重绑必要引用并保留所有共享内容。设置只更新失效的工作区引用。停止受删除影响的生成任务并等待其结束，存储写入校验所属文档/会话，避免异步回填复活已删除对象。

旧云快照不能复活有删除标记的记录。删除同步逐键执行，不按文件夹名称或路径再次递归；较新本地批注/消息/全文记录会阻止过期远端删除，保留记录时移除被拒绝的标记，使后续备份仍可正常恢复。合并造成的目录循环解除一个父链接回根目录，保留全部内容。用户主动导入本地备份可恢复其中存在的已删除记录，恢复时间晚于已知删除标记。

### 其他修改

- `openDocument` 以共享进行中 Promise 锁定第一次切换，后续点击复用该操作，不重启加载；删除后的工作区和内存缓存及时协调。
- 形状菜单选择后立即设为 shape 模式，工具图标由当前选项决定；再次点击退出。
- `chooseSaveTarget` 在所有下载入口的耗时步骤前获取目标；`saveFile` 只写该目标。两份 PDF 共用一次目录选择；缺少对应 API/返回 NotSupportedError 时使用默认下载，不弹额外提示。目标目录中已有同名文件时改名，不静默覆盖。
- `ui/mobile.js` 的按钮监听只匹配 `.mobile-nav`，避免 HTML 的状态属性被误当成导航按钮。
- 本次下载 folder-plus、folder-input、layout-grid、list、arrow-up、arrow-down、folder-tree 七个 Lucide 0.468.0 图标，未新增 npm 依赖。

### 验证

最终全部通过：`npm run check`、33 项单元测试、36 项浏览器用例、`npm run build`、`npm run test:dist`。构建仍为纯静态站点，约 16.4 MiB。

数据测试覆盖嵌套/重复选择、移动后代约束、原文/译文不同操作范围、单成员删除保留历史、最后成员删除、确认期间新增或移出的文件、新增译文、事务失败整体回滚、旧快照防复活、较新本地内容保留、目录循环合并修复、主动备份恢复及旧格式兼容。真实迁移用例从旧版七表库增量升级，验证既有 PDF 和批注仍在。

浏览器验证包含文件树弹窗内新建目录、卡片/列表和文件夹优先排序、ZIP 目录/空文件夹、两个 PDF 单次选目录、原文删除后使用译文继续完整会话、首次标签切换锁和形状即时激活。首次冷导出阻塞懒加载模块，确认选择器在加载前调用、点击授权有效且只调用一次；无选择器时下载到默认位置且无提示。桌面与手机文档管理/移动弹窗截图已查看。

首轮直接导入设置的测试曾受到长期运行开发服务器的热更新缓存影响，干净进程复测排除；移动页面被切回阅读则是根 HTML 状态属性误绑定点击监听，已修复并回归。测试使用临时浏览器配置和合成 PDF，不操作用户真实文档。PDF.js/fontkit 的已有字体兼容警告仍可能出现，中文导出回读及原有字体回归通过。

## 0.1.3 · 2026-09-22

仅围绕本轮五项反馈修改阅读器、批注交互、导航/搜索、历史按钮和必要的共用几何。未改变 LLM/词典接口、会话上下文、同步协议、数据库版本或已有数据；无新增依赖。

### 对齐定位与修复

使用合成 PDF 的实际字形推进量、页面 Canvas 像素与 DOM Range 测量复现错位：一段 Times 文本在测试缩放下实际宽约 291.92 CSS px，旧文字层宽约 308.30 px。原因包括 PDF 画布与 HTML 通用替代字体/语言的测量差异、缺失的旋转规则、依赖未定义 CSS round 参数的尺寸以及位置舍入。

新增 `pdf-text.js`，在锁定的 PDF.js TextLayer 基础上使用画布同源字体信息和文档语言，按 DOM 实测宽度匹配 PDF 段推进量，计算准确基线/位置和页面尺寸；`pdf-text-layer.css` 增加 90/180/270 度规则。未使用固定偏移补偿或修改 PDF.js 依赖包。`pdf.js` 统一页面渲染 Promise，避免搜索跳转在文字层尚未完成时读取空位置。

### 交互与结构

- `shapes.js` 新增点到线段距离、形状命中和页内位移裁切；`pdf.js` 增加指针捕获拖动，预览不提交数据库，松手原子保存、取消回滚，并记录可撤销的 before/after。便签移动保留原文选区锚点；形状透明命中层配置 touch-action，支持原生触控拖动。
- 所有历史操作完成后通知按钮状态；空历史或正在提交时禁用。撤销/重做调用页面内定位，移动操作分别定位撤销前/重做后坐标，选区移除保存实际选区位置。
- 新增 `pdf-search.js`：提取文本索引与原始 item/字符偏移映射，字面匹配和 Unicode 全字边界，不把搜索字符串当正则代码。
- `ui/pdf-navigation.js` 增加紧凑页签、搜索输入/开关、环形进度、单行结果；支持取消过期查询，结果与 PDF 关联。`pdf.js` 用真实文字层 Range 计算命中矩形，全部高亮独立绘制，缩放可重建，不写入存档。
- `mobile.css` 移除上一版窄屏强制左侧栏/分列规则，使用绝对定位浮窗；`ui/mobile.js` 只在窄屏处理外部点击关闭。桌面保留已有导航分列，查找模式略加宽以容纳同一行的两个筛选开关。

### 验证

新增 `tests/search-geometry.test.mjs`、`tests/e2e/reader-refinements.spec.js`；更新上一版移动导航用例以匹配用户此次指定的浮窗行为。最终通过 `npm run check`、17 项单元测试、28 项浏览器用例、`npm run build` 和 `npm run test:dist`。

对齐测试使用 Times、Helvetica Bold、Courier Oblique 三种字体，分别覆盖 25%、50%、75%、100%、150%、200%、400%，并含 90°/270° 旋转、非零裁切框、UserUnit=1.5；比较完整行、部分单词的推进量/位置和画布墨迹范围。另以 DPR=2 和嵌入 Noto Sans SC 验证中文文字层。触控采用 Chrome CDP 原生触控事件模拟，未进行实体手机测试。搜索测试检查大小写/全字条件、跨页全部结果、目标矩形可见性及不写入批注；拖动测试检查保存/刷新恢复、页内移动边界、擦除形状、跨页历史定位和按钮禁用。

生产构建仍约 16.3 MiB；无后端子路径部署、字体/Worker 本地加载和中文 PDF 导出通过。已查看桌面搜索与移动浮窗截图。中途两次执行审批等待超时，均按工具提示重试成功，没有改用外部代理或读取用户私密数据。

## 0.1.2 · 2026-09-22

仅覆盖本轮六项优化及其必要的保存/导出/验证逻辑。数据库版本、消息上下文、API 请求格式、同步协议未调整；不清理或重置现有文档和设置，没有新增 npm 依赖。

### 模块变动

1. `ui/assistant.js`：空助手消息且当前会话仍有活动任务时显示思考环；首段输出替换占位，finally 移除任务后恢复普通状态。译文转换复用原互斥集合驱动按钮 `aria-busy`、禁用和已有 `loader-circle` 图标，渲染/切换记录时也保持一致。
2. `pdf.js`：跟踪 PDF 选择手势的 Pointer/Touch 生命周期，selectionchange 只采集范围；鼠标松手直接提交，触控结束短暂等待原生选区稳定；取消手势不翻译，并保留键盘快捷键释放处理。避免点工具栏时取消已完成选区的翻译。
3. `selection-actions.js`：注册 strike，继承与高亮/下划线相同的选择命中、局部移除和撤销规则；note 创建回调带入选择的 fontSize。
4. 新增 `shapes.js`：在 PDF 点单位中统一矩形/圆形/直线/箭头几何和 Canvas 路径；`pdf.js` 保存归一化起止坐标、颜色和 strokeWidth，拖动时预览，提交后继承已有撤销机制。`pdf-export.js` 复用相同几何，导出矢量轮廓、线条、箭头和删除线，使用记录中的实际字号/笔宽。
5. `main.js`：增加滑块和形状选择、自绘样式、工具栏入口；配置存入现有 settings 表的 `annotation-tools` 记录，不修改已有批注。新增导航栏容器及布局状态。
6. 新增 `ui/pdf-navigation.js`：PDF.js `getOutline/getDestination/getPageIndex` 解析内置书签，IntersectionObserver 按需渲染缩略图并释放临时 Canvas；切换模式、文档或关闭时取消旧任务，防止旧异步结果回填。
7. `archive.js`：扩充批注白名单和参数校验以接受 strike/shape/fontSize/strokeWidth；兼容缺省字段的旧记录。所有新内容仍走原事务和存档流程。
8. `base.css/desktop.css/mobile.css`：状态环、尺寸滑块、删除线、导航栏和紧凑图标侧栏。桌面导航占用阅读部分宽度，移动端阅读态显示窄导航；翻译/管理视图保持原单页布局。
9. 下载 `strikethrough/shapes/rectangle-horizontal/circle/gallery-vertical-end/bookmark` 六个 Lucide 0.468.0 SVG 至 `src/_logo/icons`，扩充可复现下载脚本及来源文档。PDF.js API 官方页面已下载核对，实际接口另以锁定依赖的类型和运行验证核查。

### 验证

新增 `tests/shapes.test.mjs` 和 `tests/e2e/optimizations.spec.js`：检查几何/反向拖动、删除线及批注字号、鼠标/触控按住不请求、思考状态、转换忙碌反馈、命名书签跳页、导航互斥/宽度变化、尺寸偏好、四种形状、存档回读及实际 PDF 导出。

最终验证全部通过：`npm run check`、15 项单元测试、22 项 Chrome 浏览器用例（含原 16 项和新 6 项）、`npm run build`、`npm run test:dist`。新增内容已验证刷新恢复、备份校验、真实 PDF 导出与中文文本提取；桌面与 390×844 手机视口截图已查看，导航展开后手机 PDF 无横向溢出。触控通过浏览器触控 Pointer 事件模拟，未执行实体手机设备测试。LLM 状态使用可控响应验证，不调用用户真实账号。

生产产物约 16.3 MiB，仍为无后端静态文件；新图标直接打包。PDF.js 的已有中文子集字体类型警告仍出现，但导出读取及中文文本提取通过，与本次状态/工具改动无关。

## 0.1.1 · 2026-09-22

范围限定为用户反馈的九项问题。未改动数据库 schema、存档格式、原文/译文关系、聊天历史组织、WebDAV 或 PDF 导出内容格式；依赖未新增。

### 本次模块修改

- 新增 `selection-actions.js`：动作注册、归一化选区命中、选区矩形相减、按页面生成变更计划；`pdf.js` 使用一个 annotations 事务执行变更，并把多页变更合为一条撤销记录。`main.js` 根据选区内现有标记更新多个按钮状态，pointerdown 保留选区，添加/清除完成后释放。
- `translation.js` 并行请求 Free Dictionary 与 Wiktionary REST 定义，分别补充中文和词形，通过回调立即显示已获得结果。实测 Free Dictionary 当前环境超时，Wiktionary REST 返回 200、`Access-Control-Allow-Origin: *`，备用词典无需密钥或 LLM。
- `utils.js` 将选文件与写文件异常分开处理；仅在文件选择器不可用或缺少调用条件且尚未选择文件时退回浏览器下载。`main.js` 加保存互斥，避免双击并发；全文译文导出按记录互斥，服务端已返回 PDF 时不再进入本地生成/保存分支。未把所有保存失败都吞掉并重新下载。
- `llm.js` 保留官方嵌套文件格式；只有 400 且消息匹配 `file must have a file_id or file_data` 才把同一份文件转换为扁平字段重试一次。使用 OpenAI Docs 核对 [文件输入格式](https://developers.openai.com/api/docs/guides/file-inputs)；网关兼容分支以用户提供的错误和协议回归验证，未使用真实账号调用。
- `pdf.js` 缩放以 `max(DPR, 2, 1.5 / scale)` 计算超采样倍率，再受 600 万像素预算约束；使用实际整数像素宽高计算变换。文字层仍用 CSS viewport。尺寸和 DPR 未变时跳过延迟布局，避免选区被无效重绘清空。
- `assistant.js` 保存每份 PDF 的全文折叠状态：首段内容持久化后折叠，sticky 状态栏可停止、展开、再收起；已手动展开不会被下一段强制折叠。
- `components.js` 增加锚定浮层，复用原弹窗焦点和关闭逻辑；翻译引擎下拉枚举预置 MyMemory 和全部 `chatProviders`。`translationProviderId` 是新增可选偏好，不改变旧默认 API 字段语义。
- `base.css` 调整原文字号/行距、单行卡片、折叠动画和浮层；`desktop.css` 与 `mobile.css` 分别处理布局。手机选择后不再强制切走阅读页，以便执行新的选区按钮操作。

### 测试

新增选区规则与保存单元测试，并扩充真实浏览器测试：无选区空操作、多种标记独立切换、局部删除/撤销恢复、主词典不可用、单次保存、50%/75%/100% 像素密度、设置锚定及指定 API 路由、首段流式自动折叠及滚动吸顶。旧 PDF、存档、对话、导出测试继续保留。

验证通过：`npm run check`、13 项单元测试、15 项完整浏览器回归；随后补充的“服务端直接返回 PDF 只保存一次”浏览器用例也通过，共 16 项。`npm run build` 和 `npm run test:dist` 通过，最后构建产物约 16.3 MiB。手机新增测试验证了选择后保留阅读页、标记一次后释放、单行卡片触控高度，以及按钮下方浮层和单行菜单。真实网关账号未提供，400 重试通过复制用户错误的模拟接口测试验证。

已实际查看折叠全文结果、桌面紧凑卡片、手机翻译浮层与生产高亮截图。静态检查刻意禁用外部网络，划词会显示网络错误，这是该检查环境预期行为；PDF 渲染、中文导出和高亮均由本地资源完成。

## 0.1.0 · 2026-09-22

从只有 `.codex/pdf_translater_dev.md` 与 UI 规范的目录建立静态前端工程，产品名“纸间 · Paper Bridge”。原“海姆休息室”项目始终只读。

### 架构与依赖

- 原生 ES Modules + Vite；没有运行时后端、前端大框架或服务器数据库。
- PDF.js 6.3.289 提供 Worker 渲染和可选中文字层；只引入必要的文字层 CSS，防止上游全局 `.sidebar`、工具条规则污染本应用。
- 单页画布上限为 600 万像素，避免 4K / 高 DPI 下内存随像素比无界增长。Prettier 仅作为开发依赖整理新增代码，不进入运行时。
- pdf-lib + fontkit 延迟加载，添加批注时使用随应用部署的 Noto Sans SC 字体；html2canvas 仅在译文 PDF 排版时加载。
- markdown-it + texmath + KaTeX + highlight.js 渲染 Markdown，DOMPurify 清理不安全 HTML 与 CSS。远程图片不发起隐式请求。
- idb 管理单库七表；fflate 处理 ZIP，Web Crypto 处理 SHA-256 与 AES-GCM。
- 下载 48 个 Lucide 图标、2 个 Fluent Emoji 插画，全部保存在 `src/_logo`，保存许可证及来源。没有自行绘制图标。

### 结构与行为变更

1. 新增 `src/js/main.js` 应用协调层，连接导入、标签、工具栏、文档管理及历史入口。
2. 新增 `pdf.js`，使用可见页缓冲加载和离屏画布释放；使用归一化页面坐标保存选区、文本和手绘，导出时经 PDF.js 的 viewport 矩阵转换到 PDF 坐标。
3. 新增持久化、API 配置、翻译、LLM、Markdown、备份与 WebDAV 模块。原文与译文通过 `rootId` 共享对话；每条消息独立落盘，流式增量提交成功后显示。
4. 新增 `ui/assistant.js` 的划词、全文、问答三种视图；通过文档/会话 ID 绑定任务，异步完成时不会写入其他文档的面板。
5. 新增 `ui/settings-panel.js` 的多 API 配置、模型获取、连接测试、首次向导、备份和 WebDAV 设置。
6. 新增桌面及移动端独立样式和交互；窄屏通过底部导航切换单页，桌面分栏提供拖动和键盘调整。
7. 新增 Playwright 浏览器回归与 Node 数据/协议测试；新增静态资产准备、素材下载和语法检查脚本。
8. 保存兼容旧 `chatProviders`、`defaultChatProviderId`、`apiKey`、`baseUrl`、`model` 的数据字段；从原项目 ZIP/JSON 只迁移模型配置，未携带无关角色功能。

### 数据不丢失约束

- 数据库升级只创建表，不清表。原始 PDF 是不可变 Blob；批注单独存储；导出 PDF 为副本。
- 所有写入使用可等待的事务。失败必须报告，不以静默内存降级伪装为保存成功。
- 导入先解密、解压、校验、验证跨表引用，再单事务合并；事务错误整体回滚。
- 冲突内容保留恢复副本，重复合并使用确定性副本 ID 去重。不用旧备份静默覆盖所有本机设置或有效密钥。
- WebDAV 通过强 ETag 和条件写入避免并发覆盖；云端同步不会自动覆盖本机连接配置。
- 不使用 `max_tokens` 对正文或结构化结果强行截断。SSE 中断、`length`、`incomplete` 等状态显式保留为部分结果。
- 不能承诺关闭页面后继续接收网络内容，也不能防止用户主动清空浏览器数据；产品文档如实说明。

### 开发中定位并修复

- PDF.js 6 的 `PDFDocumentProxy` 不再提供旧 `destroy()` 接口，通过加载任务适配释放逻辑。
- PDF.js 完整查看器 CSS 覆盖应用侧栏，改为来源明确的独立文字层样式。
- 页码跳转不再减去不同定位父级的偏移量。
- 移动底栏从文档管理返回阅读时，同步恢复工作区显示状态。
- 从普通本地备份恢复设置可合并 API 配置；不包含密钥的备份仍保存非敏感设置。
- 文本清洗保留数学上标和连续年份，只删除可识别的连续行号。
- 选区坐标逐个裁切实际文字 span，不将跨页容器的大矩形当成文字选区。
- 修复高亮 CSS 自定义属性写入方式，增加实际背景色断言和生产截图检查，保证高亮可见且不遮糊文字。
- 对模型输出的远程图片在插入 DOM 前转换/过滤，避免先加载再移除。

### 验证状态

已通过 `npm run check`、8 项 Node 测试、8 项 Chrome 端到端测试和 `npm run build`。最后的高亮修复另执行受影响浏览器用例及生产检查，均通过。

浏览器测试覆盖真实 PDF 渲染、文字选择、高亮/中文文本框保存、刷新恢复、词典路由、完整多轮上下文、独立对话/跨文档隔离、原文/译文关联、KaTeX/表格/代码与 HTML 清理、实际译文 PDF 生成、中文批注 PDF 的文本提取、笔迹擦除/撤销、加密存档在全新浏览器中的恢复，以及 320×640、390×844、768×1024、1024×768、1280×720、1536×864、1920×1080、2560×1440、3840×2160 视口。高缩放通过等效 CSS 视口覆盖，未控制每种浏览器的原生缩放开关。

`npm run test:dist` 在 `/paper-bridge/` 子路径用普通静态文件服务器运行实际 dist，禁用所有外部网络请求，验证本地 Worker、图标和字体加载、PDF 导入、中文批注导出（45,086 字节的有效 PDF）、刷新后恢复、高亮实际颜色。产物为 273 个静态文件、约 16.3 MiB；主 JS gzip 约 433 KB，导出相关模块延迟加载。生产预览保存在 `test-results/production-preview.png`。

PDF.js 在读取 fontkit 子集中文字体时输出过字体 Type/Subtype 检测警告，但生成文件可打开，中文文本提取与再次渲染验证通过。Windows 执行 Node/浏览器子进程需要超出受限沙箱的执行权限，已通过自动审批运行；没有用户数据或服务凭据参与测试。

真实 MyMemory 接口已返回 `Hello world → 你好世界`；Wiktionary 端点可连通。Free Dictionary 真实端点在当前执行环境曾超时，UI 已提供超时错误。未提供真实 LLM 或 WebDAV 账号，所以未执行带用户凭据的远程联调。

### 已知边界

本地译文 PDF 为视觉排版栅格输出；扫描件没有本地 OCR；没有原文段落重排编辑；加密 PDF 的批注导出受 pdf-lib 限制；原生 PDF 输出依赖服务商 Code Interpreter 与文件内容 API；超出模型上下文的完整文档/历史不会被自动裁剪。Tauri 安装包未生成。

以后每次功能/结构修改必须同步 README、DEVELOP、STRUCTURE；版本号变更时同步 CHANGELOG。
