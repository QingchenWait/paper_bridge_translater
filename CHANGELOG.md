# 版本记录

## 0.2.0 · 2026-09-23

- 文档管理新增嵌套文件夹、路径导航、卡片/列表视图、名称/导入时间和正反序排序；文件夹始终优先。
- 新增左上角多选、文件树移动窗口及目标目录内新建子文件夹；原文移动时关联译文跟随，新译文使用原文最新目录。
- 新增批量下载：超过两份 PDF 或包含文件夹时生成保留层级的 ZIP，处理重名和空目录；下载/删除不联动未选配对文件。
- 删除按确认清单与当前选区的交集执行，保护新增/移出内容，在单事务内处理文件、编辑记录和共享历史；保留任一组成员就保留对话。
- IndexedDB 增量升级到版本 2，新增 folders/deletions；备份格式版本 2，兼容读取版本 1。同步采用精确删除标记，并保守保留较新本地内容。
- PDF 标签切换只执行首次点击直至完成；选择形状直接激活工具并切换对应图标。
- 下载提前选址以保留首次点击授权；不支持选择器时按用户要求静默下载到默认位置，取消和写失败不重复下载。
- 修复移动导航把根元素当作按钮监听，导致新文档管理被点击事件切回阅读页的问题。
- 新增 7 个下载的 Lucide 图标、文档管理/安全边界/无损升级和下载回归测试，同步四份中文文档。

## 0.1.3 · 2026-09-22

- 修复文字选区与画布错位：使用 PDF 实际字体及一致语言、精确页面尺寸和基线，校正 DOM 字宽，补齐旋转/裁切/UserUnit 换算；覆盖 25%–400% 缩放验证。
- 批注、文本框和形状支持鼠标/触控拖动，限制在本页内、松手保存、可撤销；橡皮擦增加形状命中及笔迹线段命中。
- 窄屏 PDF 导航改为阅读区上层浮窗，保留底部功能导航、保持 PDF 宽度不变，点击外部关闭。
- 统一“书签 | 缩略图 | 查找”页签；查找支持区分大小写、全字匹配、扫描进度、全部结果单行列表和精确定位；临时浅黄高亮独立于用户批注。
- 撤销/重做自动跳转对应操作位置，无可用历史或操作提交期间按钮禁用。
- 同步结构文档与定点测试；没有新增 npm 依赖、数据库版本或清理用户数据。

## 0.1.2 · 2026-09-22

- AI 问答等待首段回复期间显示“正在思考”环形进度，回答、停止或异常时退出。
- 划词翻译改为鼠标/触控松开后触发，拖动中的 selectionchange 只刷新选区状态，避免重复请求；保留键盘选区支持。
- 译文 PDF 转换/打开按钮显示统一风格旋转图标和禁用状态，完成或失败后恢复。
- 批注及文本框颜色菜单增加 6–48 pt 字号滑块；手绘增加 0.5–12 pt 笔宽滑块，显示实时数值，保存偏好和每条标记实际尺寸。
- 添加删除线工具，复用选区添加/局部清除规则；添加矩形、圆形、直线、箭头及颜色菜单，支持预览、保存、撤销、备份恢复和 PDF 导出。
- 添加互斥的缩略图/内置书签导航栏：打开时功能侧栏只显示 Logo 和 6 个图标，PDF 适应宽度重新计算，关闭恢复布局；同步适配手机。
- 下载 6 个 Lucide 图标，复用 PDF.js 缩略图/书签 API 和 pdf-lib 图形导出，没有新增运行时依赖。

## 0.1.1 · 2026-09-22

本次仅围绕反馈的九项问题修复，没有修改数据库版本、云同步协议或对话上下文机制。

- 下划线、高亮、批注统一为选区优先的一次性动作；无选区不执行，已有标记反馈按下状态，支持选区内局部清除、多类型独立切换与跨页整组撤销。
- 抽出可注册的 `selection-actions.js`，后续同类工具可复用创建、命中判断、清除及状态规则。
- 词典并行查询 Free Dictionary/Wiktionary，中文和词形独立增量显示，修复主词典超时导致整项查询失败。
- 保存 PDF 防止重复触发；文件选择、取消及写入失败路径不再重复下载；服务端直接返回 PDF 时不会再走一遍本地 PDF 保存。
- 缩小翻译右栏原文字号与行距；文件切换卡片缩为单行并保留移动触控尺寸。
- 针对文件字段缺失的 400 错误，为 Chat 兼容网关添加一次精确匹配的扁平字段重试，保留完整 PDF 内容。
- 低倍缩放使用超采样画布和精确像素变换；避免未变化尺寸触发延迟重排、清空刚选中的文字。
- 全文首段输出时自动动画折叠参数，改为可展开的吸顶进度栏，保留停止操作。
- 翻译设置改为按钮下方的紧凑浮层，列出 MyMemory 与所有已配置 LLM API，单独保存翻译服务选择。
- 同步桌面与移动端布局、用户说明、结构记录、开发记录和定点回归测试。

## 0.1.0 · 2026-09-22

### 新增

- 纯静态 PDF 阅读翻译应用：本地导入、拖放、多标签、文档搜索与重命名、页码导航、缩放及阅读进度恢复。
- 下划线、浅色高亮、批注、中文文本框、手绘、笔迹橡皮擦、会话内撤销/重做和带批注 PDF 导出。
- 免费在线词典、中文释义、词性、发音、近义词与词形补充；免配置句子和段落在线翻译。
- LLM 学术风格翻译、排版清洗、目标语言与风格设置。
- 全文文件/文字输入、流式 Markdown、公式、表格、代码和安全 HTML 样式渲染。
- 本地排版或支持服务商的原生 PDF 输出，译文 PDF 入库并共享原文对话。
- 每份文档的多个独立 AI 会话、全历史上下文、流式持久化、跨标签隔离和未完成内容恢复。
- 多 API 设置、启动配置向导、模型列表、连接测试及海姆休息室设置迁移。
- ZIP 完整备份、可选 AES-GCM 加密、SHA-256 校验、事务导入、冲突恢复副本及 WebDAV 条件同步。
- 桌面/移动独立布局、外部开源图标与插画、完整中文文档与自动化验证工程。

### 修复（初版验收中）

- 隔离 PDF.js 样式，修复侧栏被遮挡及上游全局样式污染。
- 适配 PDF.js 6 文档销毁 API，修复全文提取和对话初始化异常。
- 修正页码定位与手机底部导航返回阅读的行为。
- 防止 SSE 结束标记缺失时把中断结果标为完成。
- 保留数学上标、数值和年份，避免轻量排版清洗破坏技术内容。
- 修复高亮颜色 CSS 变量未生效；逐文字 span 计算多页选区；限制高分屏单页画布像素数。
- 在插入 DOM 前替换 Markdown 图片，防止隐式外部图片加载。

### 边界

- 尚未交付 Tauri 安装包或本地 OCR。
- 本地译文 PDF 不保留可搜索文字层和原文精确版式；原生模型 PDF 能力取决于接口。
- 真实 LLM/WebDAV 账号联调未执行，网络与 CORS 限制见 README。
