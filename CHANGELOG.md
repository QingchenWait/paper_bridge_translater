# 版本记录

## 0.3.2 · 2026-09-23

文件管理同版本修订：

- 新导入/生成的 PDF 保存原始 MD5；已有同指纹时询问是否继续，确认后在后缀前追加“副本”，每份手动上传文件保持独立的编辑和对话。
- 旧记录按需补算指纹，备份/恢复/云同步保留该字段；批量与同页面并发导入串行处理，支持 Web Locks 时跨标签页防止绕过重复确认。
- 从阅读页进入文档管理时定位当前 PDF 的最新目录；没有活动 PDF 时进入根目录。桌面与移动端一致，版本保持 0.3.2。
- 71 项单元测试、89 项浏览器回归、构建和生产静态检查通过，四份开发文档已同步。

初始 v0.3.2 更新：

- 新增客户端优先使用的有道 suggest 词典，按词性标记解析中文释义，过滤不匹配的补全结果；网页版自动跳过，基础设置注明“网页版不支持”。
- 预留客户端 CORS 请求钩子，失败时自动继续其他词典；代理随后续客户端打包接入。
- 详细释义须包含有效词性与解释，只有简短中文或空释义时继续遍历备用词典。
- 英文解释先通过可用基础翻译 API 转成中文再显示，词性显示中文；支持服务失败回退、相同解释去重及取消，结果注明翻译来源，不调用 LLM。
- 无法转换的英文不会直接显示为最终详细释义；继续尝试其他词典，全部失败时保留已有词义并明确反馈。
- 同步四份开发文档，66 项单元测试、82 项浏览器回归、构建及生产静态部署检查通过。

## 0.3.1 · 2026-09-23

同版本修订：

- 单词中文释义缺失时，依次查询 FreeDictionaryAPI.com、3325 词典；已有结果优先保留，无需 Key、不调用 LLM。
- 适配词性、释义、音标以及接口提供的例句、近义词和词形，显示来源/许可证；取消旧选词时停止备选链路。
- 仅窄屏隐藏文件切换卡片行右侧的重复打开按钮，保留顶栏入口；顶栏标题改为“纸间 · 文献翻译”。桌面布局和版本号保持不变。
- 62 项单元测试、73 项浏览器回归、构建与生产静态检查通过；两家词典均已实测浏览器跨域获取中文，同步四份开发文档。

初始 v0.3.1 更新：

- “打开 PDF”改为按钮下方的三项菜单：本地文件、APP 文档库树、外部链接；链接校验 `.pdf` 后缀，支持固定后缀重命名，验证成功后存入根目录并打开。
- 下划线、删除线、高亮导出为标准 PDF 文字标记；每笔连续手绘导出为一个 Ink 铅笔注释，支持其他具备注释编辑能力的阅读器整体处理。
- 修复 NotoSansSC 字体子集的 CFF 文件头和字形字典/子程序映射错误；连续编辑导出保留原字体，旧版损坏子集从原 Noto 字体重建，不替换字体。
- 批注/文本框浮栏直接依附对象，拖动同步跟随；文字编辑时新增仅含字号增减、删除的三按钮浮栏，删除可撤销。
- 浏览器标签标题更新为“纸间 · 文献翻译”，桌面与移动端同步适配；无数据库、存档格式或运行依赖升级。
- 58 项单元测试、完整 66 项浏览器回归、构建和生产子路径检查通过，同步四份开发文档及字体适配许可证。

## 0.3.0 · 2026-09-23

原位批注修订（版本号保持 v0.3.0）：

- 批注/文本框改为页面原位输入，支持换行、自动尺寸、单击尺寸模式和双击编辑；退出时空内容自动移除。
- 新增左右宽度手柄及跟随浮栏：字号增减、文本框边框、自动尺寸重置、可撤销删除；保留鼠标/触控拖动。
- 手动宽度、边框和多行文字实时保存，继续编辑保持手动宽度，备份/导出等待写入完成。
- 批注源文新增同色下划线及悬停发光；导出改为标准 PDF 注释与 Popup，批注文字不再合并进正文。
- “模型与 API”输入框和下拉更紧凑，样式仅限该页；同步四份文档及四个下载图标。
- 52 项单元、59 项浏览器用例和静态部署检查通过。Chrome 原生弹窗的中文字体限制已在 README/DEVELOP 中明确，文件中的 Unicode 内容完整。

同版本修订（版本号保持 v0.3.0）：

- 修复减少动态效果模式下环形进度以 0.01ms 无限循环的问题，统一正常旋转速度。
- 模型与 API、基础翻译配置改为无提示实时自动保存；共用写入队列保护交错配置，避免覆盖或丢失最后输入。
- “添加”左侧新增“保存当前设置”，保存模型及选项并保持窗口打开。
- 备份等待在途配置写入；勾选包含凭据时完整保存各模型 Key、基础翻译 Secret、WebDAV 账号及其他已配置项。
- 启动窗口更新标题/副标题、作者新标签链接、居中进入按钮、右下角“不再显示”及右上角关闭按钮。
- 本次同版本修订已通过 47 项单元测试、完整 53 项浏览器用例及生产静态部署检查，同步四份文档。

基础翻译功能更新：

- 设置新增“基础翻译功能”子页，支持自绘默认模型选择、三家服务的单行折叠配置、图文链接教程、连接测试、密钥显示切换与独立保存。
- 新增免 Key Google 在线翻译，并保留标注每日额度较低的 MyMemory；完整保存的百度、阿里云、火山配置会加入默认及阅读区引擎菜单。
- 接入百度论文领域/通用翻译、阿里 TranslateGeneral、火山 TranslateText；“学术论文”使用百度 academic 中英领域，其余风格使用通用接口，均不调用 LLM。
- 为静态部署实现百度官方 JSONP、三种签名、语言代码映射、分段、超时/取消和错误提示；修改凭据会废弃旧连接测试，成功状态注明测试时间。
- 基础翻译凭据随完整存档保存；普通备份与云同步剥除 ID/密钥，导入不会清空现有凭据或混用不同账号，多个配置同时保存按顺序合并。
- 新增轻量 @noble/hashes 2.4.0 用于 MD5，其他签名使用 Web Crypto；同步桌面/移动端、许可证及四份文档。44 项单元、47 项浏览器覆盖经全量及定点复测通过，构建和生产静态检查通过。

## 0.2.1 · 2026-09-23

- 修复文档管理下载未包含编辑的问题：单份、两份、目录 ZIP、阅读栏和已有译文 PDF 下载统一合并当前未删除的编辑记录，原始文件及可继续编辑的状态不被覆盖。
- 形状菜单在 PDF 浏览编辑区按下时立即收起并激活当前形状，鼠标/触控可直接完成首笔绘制。
- 从原文开始取词后，覆盖其上的批注、文本框、形状及笔迹暂停指针交互，松手后恢复，避免打断选区或误拖动。
- 启动引导增加默认关闭的“不再显示”自绘开关；仅手动开启后跳过后续引导，保留旧版 API 配置。
- API Key 增加明文/隐藏切换及“获取”官网入口，读取尚未保存的 Base URL，未知地址禁用；支持网页新标签及 Tauri 默认系统浏览器桥接。
- API“添加”改为带下载厂商 LOGO 的八项自绘菜单，预填 DeepSeek、MiMO、Qwen、OpenAI、GLM、Kimi、LM Studio 与自定义配置；地址和型号按指定值，已有配置不被替换。
- 增加七个 Lobe Icons 厂商 LOGO、Lucide eye-off 图标及许可证，同步四份开发文档；36 项单元与 42 项浏览器回归通过。

## 0.2.0 · 2026-09-23

- 文档管理新增嵌套文件夹、路径导航、卡片/列表视图、名称/导入时间和正反序排序；文件夹始终优先。
- 新增左上角多选、文件树移动窗口及目标目录内新建子文件夹；原文移动时关联译文跟随，新译文使用原文最新目录。
- 新增批量下载：超过两份 PDF 或包含文件夹时生成保留层级的 ZIP，处理重名和空目录；下载/删除不联动未选配对文件。
- 删除按确认清单与当前选区的交集执行，保护新增/移出内容，在单事务内处理文件、编辑记录和共享历史；保留任一组成员就保留对话。
- IndexedDB 增量升级到版本 2，新增 folders/deletions；备份格式版本 2，兼容读取版本 1。同步采用精确删除标记，并保守保留较新本地内容。
- PDF 标签切换只执行首次点击直至完成；选择形状直接激活工具并切换对应图标。
- 下载提前选址以保留首次点击授权；不支持选择器时按用户要求静默下载到默认位置，取消和写失败不重复下载。
- 修复移动导航把根元素当作按钮监听，导致新文档管理被点击事件切回阅读页的问题。
- 新增 7 个下载的 Lucide 图标、文档管理/安全边界/无损升级和下载回归测试，同步四份中文文档。

## 0.1.3 · 2026-09-22

- 修复文字选区与画布错位：使用 PDF 实际字体及一致语言、精确页面尺寸和基线，校正 DOM 字宽，补齐旋转/裁切/UserUnit 换算；覆盖 25%–400% 缩放验证。
- 批注、文本框和形状支持鼠标/触控拖动，限制在本页内、松手保存、可撤销；橡皮擦增加形状命中及笔迹线段命中。
- 窄屏 PDF 导航改为阅读区上层浮窗，保留底部功能导航、保持 PDF 宽度不变，点击外部关闭。
- 统一“书签 | 缩略图 | 查找”页签；查找支持区分大小写、全字匹配、扫描进度、全部结果单行列表和精确定位；临时浅黄高亮独立于用户批注。
- 撤销/重做自动跳转对应操作位置，无可用历史或操作提交期间按钮禁用。
- 同步结构文档与定点测试；没有新增 npm 依赖、数据库版本或清理用户数据。

## 0.1.2 · 2026-09-22

- AI 问答等待首段回复期间显示“正在思考”环形进度，回答、停止或异常时退出。
- 划词翻译改为鼠标/触控松开后触发，拖动中的 selectionchange 只刷新选区状态，避免重复请求；保留键盘选区支持。
- 译文 PDF 转换/打开按钮显示统一风格旋转图标和禁用状态，完成或失败后恢复。
- 批注及文本框颜色菜单增加 6–48 pt 字号滑块；手绘增加 0.5–12 pt 笔宽滑块，显示实时数值，保存偏好和每条标记实际尺寸。
- 添加删除线工具，复用选区添加/局部清除规则；添加矩形、圆形、直线、箭头及颜色菜单，支持预览、保存、撤销、备份恢复和 PDF 导出。
- 添加互斥的缩略图/内置书签导航栏：打开时功能侧栏只显示 Logo 和 6 个图标，PDF 适应宽度重新计算，关闭恢复布局；同步适配手机。
- 下载 6 个 Lucide 图标，复用 PDF.js 缩略图/书签 API 和 pdf-lib 图形导出，没有新增运行时依赖。

## 0.1.1 · 2026-09-22

本次仅围绕反馈的九项问题修复，没有修改数据库版本、云同步协议或对话上下文机制。

- 下划线、高亮、批注统一为选区优先的一次性动作；无选区不执行，已有标记反馈按下状态，支持选区内局部清除、多类型独立切换与跨页整组撤销。
- 抽出可注册的 `selection-actions.js`，后续同类工具可复用创建、命中判断、清除及状态规则。
- 词典并行查询 Free Dictionary/Wiktionary，中文和词形独立增量显示，修复主词典超时导致整项查询失败。
- 保存 PDF 防止重复触发；文件选择、取消及写入失败路径不再重复下载；服务端直接返回 PDF 时不会再走一遍本地 PDF 保存。
- 缩小翻译右栏原文字号与行距；文件切换卡片缩为单行并保留移动触控尺寸。
- 针对文件字段缺失的 400 错误，为 Chat 兼容网关添加一次精确匹配的扁平字段重试，保留完整 PDF 内容。
- 低倍缩放使用超采样画布和精确像素变换；避免未变化尺寸触发延迟重排、清空刚选中的文字。
- 全文首段输出时自动动画折叠参数，改为可展开的吸顶进度栏，保留停止操作。
- 翻译设置改为按钮下方的紧凑浮层，列出 MyMemory 与所有已配置 LLM API，单独保存翻译服务选择。
- 同步桌面与移动端布局、用户说明、结构记录、开发记录和定点回归测试。

## 0.1.0 · 2026-09-22

### 新增

- 纯静态 PDF 阅读翻译应用：本地导入、拖放、多标签、文档搜索与重命名、页码导航、缩放及阅读进度恢复。
- 下划线、浅色高亮、批注、中文文本框、手绘、笔迹橡皮擦、会话内撤销/重做和带批注 PDF 导出。
- 免费在线词典、中文释义、词性、发音、近义词与词形补充；免配置句子和段落在线翻译。
- LLM 学术风格翻译、排版清洗、目标语言与风格设置。
- 全文文件/文字输入、流式 Markdown、公式、表格、代码和安全 HTML 样式渲染。
- 本地排版或支持服务商的原生 PDF 输出，译文 PDF 入库并共享原文对话。
- 每份文档的多个独立 AI 会话、全历史上下文、流式持久化、跨标签隔离和未完成内容恢复。
- 多 API 设置、启动配置向导、模型列表、连接测试及海姆休息室设置迁移。
- ZIP 完整备份、可选 AES-GCM 加密、SHA-256 校验、事务导入、冲突恢复副本及 WebDAV 条件同步。
- 桌面/移动独立布局、外部开源图标与插画、完整中文文档与自动化验证工程。

### 修复（初版验收中）

- 隔离 PDF.js 样式，修复侧栏被遮挡及上游全局样式污染。
- 适配 PDF.js 6 文档销毁 API，修复全文提取和对话初始化异常。
- 修正页码定位与手机底部导航返回阅读的行为。
- 防止 SSE 结束标记缺失时把中断结果标为完成。
- 保留数学上标、数值和年份，避免轻量排版清洗破坏技术内容。
- 修复高亮颜色 CSS 变量未生效；逐文字 span 计算多页选区；限制高分屏单页画布像素数。
- 在插入 DOM 前替换 Markdown 图片，防止隐式外部图片加载。

### 边界

- 尚未交付 Tauri 安装包或本地 OCR。
- 本地译文 PDF 不保留可搜索文字层和原文精确版式；原生模型 PDF 能力取决于接口。
- 真实 LLM/WebDAV 账号联调未执行，网络与 CORS 限制见 README。
